Read root `AGENTS.md` before substantive work.

For current project/release state, use the canonical control plane in `gnabriverner-pixel/digital-code-system` (`AGENTS.md`, `STATUS.md`, product strategy, decision log). Do not create an independent Web-only strategy.

Preserve Lovable history safety: no force-push or rewriting published history.

Preserve product invariants: deterministic calculation vs interpretation separation, correction/rejection supersession, `Myth metaphor != biography`, no forced Meeting resonance padding, and semantic boldness + epistemic humility.

Use one writable executor per gate, exact SHA checks, minimal coherent changes, and browser/runtime evidence for UI/deployment claims. Never expose secrets or PII.